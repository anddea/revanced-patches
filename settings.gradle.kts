rootProject.name = "voice-over-translation-patches"

pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}

plugins {
    id("app.revanced.patches") version "1.0.0-dev.6"
}
